import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
    pagination: {
        width: "100%",
        display: "flex",
        justifyContent: "center",
        margin: '40px 0px'
    }
}))